import type { Template } from '../types';
import { DEFAULT_TEMPLATE } from './default';

export const TEMPLATES: Template[] = [DEFAULT_TEMPLATE];

export { DEFAULT_TEMPLATE } from './default';
export { resolveTemplate } from './default';
